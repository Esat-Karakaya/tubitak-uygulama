import { StyleSheet, } from 'react-native';

export default styles = StyleSheet.create({
  linkContainer:{
    flexDirection:"row",
    alignItems:"center",
    columnGap:10,
    backgroundColor:"white",
    borderWidth:1,
    borderRadius:15,
    padding:15
  }
});